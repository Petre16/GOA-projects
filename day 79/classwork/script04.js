// 6. Capitalize names
// Given an array of lowercase names, use a for...of loop to print each name with the first letter capitalized.
// Example: ['john', 'mike'] → John, Mike

let names = ["petre", "giorgi"];
for (let name of names) {
    name.toUpperCase()
    console.log(name);
}